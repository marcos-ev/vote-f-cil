rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: exige usuário autenticado para qualquer operação.
    // Endureça estas regras antes de escalar o uso.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
